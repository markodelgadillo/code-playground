const { expect } = require('chai')
const prefill = require('./index.js')

describe('', () => {
  it('', () => {
    expect(prefill(n, v)).to.eql()
  })
  it('', () => {
    expect(prefill(n, v)).to.eql()
  })
})
